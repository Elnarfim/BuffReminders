Libs/
.ignored/
# Translator-maintained files. stylua measures CJK strings as single-width and
# re-wraps lines that are already inside the column limit on screen, so every
# run churns lines no one edited.
Locales/
# Generated by scripts/generate-alert-sounds.py; the compact rows are the
# generator's output, so reformatting them would churn on every regeneration.
Data/AlertSounds.lua
